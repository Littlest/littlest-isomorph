#!/usr/bin/env node

// Transparently allow require-ing `component.jsx`.
require('node-jsx').install({ extension: '.jsx' });

var app = require('../lib/server');
var PORT = process.env.PORT || 8080;

app.listen(PORT);

console.log('Server %s listening on port %s...', process.pid, PORT);
